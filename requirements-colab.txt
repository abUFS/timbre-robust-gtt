# requirements-colab.txt
# =============================================================================
# ESTRATÉGIA (revisada após a Fase 0 real): NÃO fixar versões antigas.
#
# Descoberta: o Colab roda Python 3.12, e o stack antigo (numpy 1.23, scipy 1.10,
# numba 0.56, pandas 1.5...) NÃO tem wheel para 3.12 e não compila — por isso o
# `pip install -r` antigo falhava inteiro e nada era instalado.
#
# E é DESNECESSÁRIO: o código do amt-tools não usa nada que o numpy 2.0 quebrou
# (sem np.float/np.NaN etc.) e só chama APIs estáveis do librosa. Os frameworks
# importam e rodam no stack moderno padrão do Colab.
#
# Portanto, aqui vai SÓ o que falta no Colab. Tudo o mais (numpy, scipy, librosa,
# jams, mir_eval, mirdata, pandas, matplotlib, sacred, torch-GPU) já vem no Colab
# e satisfaz as restrições ">=" dos 3 repositórios.
# =============================================================================

pedalboard>=0.9.0     # Fases 1-2 (efeitos de áudio)
PyYAML>=6.0           # leitura dos configs/*.yaml
resampy>=0.4.3        # librosa usa p/ resample 44.1k->22.05k; opcional no librosa
                      # moderno e AUSENTE no Colab, mas o amt-tools depende dele

# Observação: muda (dep do FretNet/continuous) NÃO entra aqui — não instala no
# 3.12 e só é necessária na Fase 4. Tratamos o FretNet separadamente lá.