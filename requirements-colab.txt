# requirements-colab.txt
# =============================================================================
# PONTO DE PARTIDA — a Fase 0 vai fechar os números exatos.
#
# Os 3 frameworks são de ~2022 e declaram dependências só com ">=" (sem teto).
# No Colab moderno (Python 3.11+, numpy 2.x, librosa 0.10+) isso QUEBRA. Aqui
# fixamos a "constelação científica" frágil numa versão compatível com a época.
#
# Ordem de instalação (ver notebooks/00_bootstrap.ipynb):
#   1) pip install -r requirements-colab.txt      <- fixa o que está abaixo
#   2) pip install -e ./amt-tools ./guitar-transcription-with-inhibition \
#                     ./guitar-transcription-continuous
#      (pip NÃO faz upgrade do que já satisfaz ">=", então os pins seguram)
#   3) REINICIAR O RUNTIME (numpy foi rebaixado) e rodar src.env_check
#
# torch: NÃO fixamos aqui — usamos o torch pré-instalado do Colab (já casado
# com o CUDA da máquina). Ele satisfaz torch>=1.11.0.
# =============================================================================

# --- núcleo científico (a parte que quebra) ---
numpy==1.23.5
scipy==1.10.1
numba==0.56.4          # librosa 0.9 exige numba<0.57 -> numpy<1.24
llvmlite==0.39.1       # casado com numba 0.56.x

# --- áudio / MIR ---
librosa==0.9.2
soundfile>=0.12.1
resampy==0.4.2
audioread>=3.0.0
jams==0.3.4
mirdata==0.3.6
mir_eval==0.7

# --- dados / logging / experimentos ---
pandas==1.5.3
matplotlib==3.7.5
sacred==0.8.4
tqdm>=4.64.0
mido==1.2.10
tensorboard>=2.9.0
tensorboardX==2.6.2.2
requests>=2.27.1

# --- deps específicas dos repos de tablatura ---
pyguitarpro>=0.9       # guitar-transcription-with-inhibition
muda>=0.4.1            # guitar-transcription-continuous (FretNet)

# --- nossas ferramentas (efeitos / análise) ---
pedalboard>=0.8.0      # Fases 1-2 (efeitos de áudio)
PyYAML>=6.0            # leitura dos configs/*.yaml
